using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		print ("Some message");		
	}
	
	// Update is called once per frame
	void Update () {
		print ("This is another message per frame");
	}
}
